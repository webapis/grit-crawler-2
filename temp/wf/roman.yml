name: roman
on:
  schedule:
    - cron:  '0 0 * * 4,6'
env:
  GH_TOKEN: ${{secrets.GH_TOKEN}}
jobs:
  kadin_job:
    uses: ./.github/workflows/reusable.yml
    with:
      GENDER: kadin
      maxconcurrency: '5'
      WEBSITE: 'biraradamoda'
      marka: 'roman'
    secrets: inherit
